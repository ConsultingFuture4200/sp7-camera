[Service]
# Per-sensor tuning files (ov8865.yaml, ov5693.yaml) without touching
# /usr/share/libcamera. libcamera checks this path before the system one.
Environment=LIBCAMERA_IPA_CONFIG_PATH=%h/Projects/sp7-camera/ipa
